# 저장소 작성 규칙

## 저장소 목적

- 지능로봇실습의 주차별 코드, 실행 방법, 실습 결과를 정리한다.
- 교수님이 README에서 수행 내용과 결과를 확인하고, 필요한 경우 실행을 재현할 수 있도록 작성한다.
- 기본 실습 환경은 Ubuntu 22.04와 ROS 2 Humble이다. 주차별 추가 의존성과 설정은 해당 README에 적는다.

## 작성 원칙

- 한국어로 간결하고 구체적으로 작성한다. 명령어와 코드 식별자는 원문을 유지한다.
- 해당 주차의 과제 지시, 실제 코드, 캡처, 로그를 먼저 확인한다.
- 주차는 사용자가 지정한 분류를 따른다. 강의 계획표만 보고 기존 주차를 임의로 바꾸지 않는다.
- 코드의 의도와 실제 실행 결과를 구분한다. 확인되지 않은 실행이나 결과를 수행한 것처럼 작성하지 않는다.
- 자료가 부족하면 확인 가능한 내용부터 정리하고, 부족한 항목을 사용자에게 알린다.

## 주차별 README

- 제목은 `weekNN 실습 정리`로 통일한다. NN은 02, 03, 10처럼 두 자리 주차 번호이다.
- 기본 구성은 `목표 → 과정 → 결과`로 유지한다.

### 목표

- 해당 실습에서 구현하거나 확인할 내용을 1~3개 항목으로 작성한다.
- 실제 수행 범위를 넘어서는 목표나 일반적인 수업 소개는 넣지 않는다.

### 과정

- 실습 순서에 따라 하위 제목을 구분한다.
- 주요 코드 파일을 상대경로로 연결하고, 역할과 핵심 동작을 간략히 설명한다.
- 전체 코드를 반복해서 붙이지 않는다. 설명에 필요한 핵심 부분만 인용한다.
- 실행 명령은 bash 코드 블록으로 작성하고, 실행 위치와 사전 조건을 명시한다.
- 패키지 배치, 필요한 환경 설정, 최초 빌드, 실행 순서를 빠뜨리지 않는다.
- 여러 터미널이 필요하면 터미널별 명령과 환경 설정을 구분한다.
- 실행 스크립트를 사용하는 경우 스크립트의 역할과 핵심 ROS 2 명령을 설명한다.
- 노드명, 토픽명, 패키지명, 실행 파일명, 파라미터는 실제 코드와 일치시킨다.
- 특정 PC의 절대경로를 반복하지 않는다. 기준 경로를 설명한 뒤 상대경로나 환경변수를 사용한다.

### 결과

- 무엇을 실행했고 무엇이 관찰되었는지 먼저 짧게 서술한다.
- 과제에서 요구한 확인 항목에 대응하는 캡처·로그·보고서를 연결한다.
- 핵심 캡처는 README에 표시하고, 각 이미지에 확인할 내용을 설명한다.
- 실제 출력과 설명용 예상 출력을 명확히 구분한다.
- 미완료 항목이나 확인하지 못한 동작이 있으면 해당 범위를 표시한다.
- 코드나 설정 변경으로 기존 결과 자료가 맞지 않게 되면 최신 결과처럼 사용하지 않는다.

## 기록 범위와 파일 관리

- 경로·사용자명 변경, 문서 수정 검토, 반복 재빌드 등 유지보수 작업 이력은 주차 README에 적지 않는다.
- 실습 이해에 필요한 오류 해결은 과정 안에서 증상·원인·해결 방법만 짧게 정리할 수 있다.
- 기존 주차 폴더와 ROS 패키지 구조를 유지한다. 정리를 위해 실행 코드를 불필요하게 변경하지 않는다.
- 캡처는 `weekNN/screenshots/`, 제출용 로그는 `weekNN/results/`에 둔다.
- 기존 보고서 원본을 보존하고 README에서 연결한다.
- build/, install/, log/, __pycache__/ 등 자동 생성 파일은 커밋에서 제외한다.
- results/의 제출용 로그는 보존한다. 모든 .log 파일을 일괄 제외하지 않는다.
- 이미지와 파일 링크는 해당 README 기준 상대경로로 작성한다.
- 주차 자료를 추가하거나 변경하면 루트 README의 실습 자료 목록도 필요한 범위에서 갱신한다.

## 확인 및 업로드

- 작업 전 Git 상태, 현재 브랜치, 원격 저장소를 확인한다.
- 요청한 주차와 관련 파일만 수정하고, 기존 사용자 변경을 보존한다.
- 문서만 변경했다면 링크, 파일 존재 여부, 명령과 코드의 일치 여부를 확인한다.
- 코드를 변경했다면 가능한 환경에서 관련 빌드·실행을 확인한다. 실행하지 못한 검증은 사용자에게 알린다.
- 사용자가 업로드까지 요청했다면 검증 후 커밋·푸시까지 진행한다.
- 커밋 대상 파일을 명시적으로 선택하고, 요청과 무관한 변경이나 기존 미푸시 커밋이 함께 올라가지 않는지 확인한다.
- README 본문은 한국어로 작성한다.
- 커밋 메시지는 각 파일의 역할을 설명하는 짧고 구체적인 영어 문장으로 작성한다.
- GitHub 파일 목록에서 파일마다 적절한 설명이 보이도록 서로 다른 역할의 파일은 커밋을 분리한다. 예: `Publish numbered greetings`, `Record subscriber output`.
- 폴더에서 마지막으로 변경하는 파일의 커밋 메시지는 해당 파일과 폴더의 목적을 함께 설명할 수 있도록 작성한다.
- 강제 푸시나 기존 작업을 버리는 명령은 사용하지 않는다.
- 완료 후 변경 내용, 확인 결과, 남은 항목, 커밋·푸시 여부를 사용자에게 짧게 보고한다.
